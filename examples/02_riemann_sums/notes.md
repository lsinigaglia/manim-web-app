# Riemann Sum Notes

Key patterns:
- Use `axes.get_riemann_rectangles()` for integration visualization
- Set `dx` to control rectangle width
- Use `fill_opacity` for transparency
- Keep x_range within axes bounds
