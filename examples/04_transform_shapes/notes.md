# Shape Transformation Notes

Key patterns:
- Use `Transform()` to morph between shapes
- `FadeIn` and `FadeOut` for smooth appearance/disappearance
- Set `fill_opacity` for filled shapes
- Use `.scale()` to resize shapes
- Transform modifies the first object into the second
