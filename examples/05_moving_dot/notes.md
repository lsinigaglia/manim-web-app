# Moving Dot Notes

Key patterns:
- Use `ValueTracker` to control animation parameter
- `always_redraw()` creates objects that update automatically
- `axes.c2p()` converts coordinates to points
- `t.animate.set_value()` animates the tracker
- `rate_func=linear` for constant speed
