# Axes and Plotting Notes

Key patterns:
- Always use `Axes` for coordinate systems
- Use `axes.plot()` for function graphs
- `Create()` for drawing objects
- `Write()` for text/labels
- Keep x_range and y_range reasonable for visibility
