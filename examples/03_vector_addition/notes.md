# Vector Addition Notes

Key patterns:
- Use `Arrow` for vectors
- `NumberPlane` for coordinate grid
- `GrowArrow` animation for arrows
- `MathTex` for mathematical labels
- Position labels with `.next_to()`
